server:
  port: 10086
spring:
  application:
    name: ly-registry
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka
    # 是否注册eureka自身
    register-with-eureka: false
    # 是否需要拉取自身的服务列表
    fetch-registry: false